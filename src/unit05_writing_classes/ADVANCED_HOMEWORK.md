# Advanced Homework

- **Banking System:** Design a complete `BankAccount` system with `Customer` and `Account` classes. Track balances, prevent overdrafts, and maintain a static variable for total money in the bank.
- **Fraction Calculator:** Build a `Fraction` class that can add, subtract, multiply, and divide two fraction objects, reducing the resulting fraction to its simplest form.
