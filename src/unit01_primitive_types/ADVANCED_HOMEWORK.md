# Advanced Homework

- **Keyboard Input:** Write a program that uses `Scanner` to read user input (integers and doubles) from the keyboard to perform calculations (e.g., calculating the area of a circle, compound interest, or a student's GPA).
- **File Input:** Create a program that uses `Scanner` and the `File` class to read primitive data values from a text file, compute their sum and average, and output the results.
