# Advanced Homework

- **To-Do List Manager:** Build a functional To-Do list application allowing the user to add, remove, insert at a specific index, and search for tasks dynamically using an `ArrayList`.
- **Sieve of Eratosthenes:** Implement the "Sieve of Eratosthenes" algorithm using an `ArrayList` to find all prime numbers up to a user-specified limit.
