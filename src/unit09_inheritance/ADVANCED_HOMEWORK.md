# Advanced Homework

- **Payroll System:** Build an `Employee` payroll system with different types of employees (`SalariedEmployee`, `HourlyEmployee`, `CommissionEmployee`) extending a base `Employee` class, demonstrating polymorphism in calculating their monthly pay.
- **Animal Hierarchy:** Design an animal kingdom hierarchy to demonstrate overriding methods (like `makeSound()` or `move()`) and the use of the `super` keyword.
